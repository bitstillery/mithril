{
  "name": "mithril-docs-site",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "bun run build.ts",
    "dev": "bun --hot server.ts",
    "start": "bun run server.ts"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/bun": "latest",
    "marked": "^14.0.0"
  }
}